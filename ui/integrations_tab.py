from PyQt5.QtWidgets import QWidget, QVBoxLayout, QLabel, QLineEdit, QPushButton, QTextEditfrom PyQt5.QtGui import QFontclass IntegrationsTab(QWidget):    def __init__ (self, integrador):        super().__init__()        self.integrador = integrador        layout = QVBoxLayout()        titulo = QLabel("IntegraÃ§Ãµes de Softwares")        titulo.setFont(QFont("Arial", 14, QFont.Bold))        layout.addWidget(titulo)        self.input_nome = QLineEdit()        self.input_nome.setPlaceholderText("Nome do software (ex: Dialogflow)")        layout.addWidget(QLabel("Nome do Software"))        layout.addWidget(self.input_nome)        self.input_funcionalidades = QLineEdit()        self.input_funcionalidades.setPlaceholderText("Funcionalidades (separadas por vÃ­rgula)")        layout.addWidget(QLabel("Funcionalidades"))        layout.addWidget(self.input_funcionalidades)        btn_conectar = QPushButton("ðŸ”— Conectar Software")        btn_conectar.clicked.connect(self._conectar_software)        layout.addWidget(btn_conectar)        btn_listar = QPushButton("ðŸ“‹ Listar Softwares Conectados")        btn_listar.clicked.connect(self._listar_softwares)        layout.addWidget(btn_listar)        self.output_softwares = QTextEdit()        self.output_softwares.setReadOnly(True)        layout.addWidget(QLabel("Softwares Conectados"))        layout.addWidget(self.output_softwares)        self.setLayout(layout)    def _conectar_software(self):        nome = self.input_nome.text().strip()        funcionalidades = [f.strip() for f in self.input_funcionalidades.text().split(",") if f.strip()]        if not nome or not funcionalidades:            self.output_softwares.setText("âš ï¸ Nome e funcionalidades sÃ£o obrigatÃ³rios.")            return        self.integrador.conectar(nome, funcionalidades)        self.output_softwares.setText(f"Software {nome} conectado com sucesso.")    def _listar_softwares(self):        output = "Softwares conectados:\n"        for sw in self.integrador.softwares_conectados:            output += f"- {sw['nome']} com funcionalidades: {', '.join(sw['funcionalidades'])}\n"        self.output_softwares.setText(output)