from PyQt5.QtWidgets import QApplication, QWidget, QLabel, QLineEdit, QPushButton, QVBoxLayout, QSpacerItem, QSizePolicyfrom PyQt5.QtGui import QPixmap, QPalette, QBrushfrom PyQt5.QtCore import Qtimport sysclass LoginWindow(QWidget):    def __init__(self):        super().__init__()        self.setWindowTitle("Login")        self.setGeometry(200, 100, 800, 500)        self.init_ui()    def init_ui(self):        # Background image        background = QPixmap("ui/assets/logo.png")        palette = QPalette()        palette.setBrush(QPalette.Window, QBrush(background.scaled(self.size(), Qt.KeepAspectRatioByExpanding)))        self.setPalette(palette)        self.email_label = QLabel("Email:")        self.email_input = QLineEdit()        self.email_input.setPlaceholderText("comercial.jorge.anjos@gmail.com")        self.password_label = QLabel("Senha:")        self.password_input = QLineEdit()        self.password_input.setEchoMode(QLineEdit.Password)        self.password_input.setPlaceholderText("212140043")        self.login_button = QPushButton("Entrar")        self.login_button.clicked.connect(self.check_login)        layout = QVBoxLayout()        layout.addSpacerItem(QSpacerItem(20, 180, QSizePolicy.Minimum, QSizePolicy.Expanding))        layout.addWidget(self.email_label)        layout.addWidget(self.email_input)        layout.addWidget(self.password_label)        layout.addWidget(self.password_input)        layout.addWidget(self.login_button)        layout.setAlignment(Qt.AlignCenter)        self.setLayout(layout)    def check_login(self):        if (self.email_input.text() == "comercial.jorge.anjos@gmail.com" and                self.password_input.text() == "212140043"):            print("Login bem-sucedido!")            self.close()  # Aqui entra o dashboard        else:            print("Credenciais invÃ¡lidas.")if __name__ == "__main__":    app = QApplication(sys.argv)    login_window = LoginWindow()    login_window.show()    sys.exit(app.exec_())