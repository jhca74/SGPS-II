from PyQt5.QtWidgets import QWidget, QLabel, QVBoxLayoutimport psutilclass DiagnosticoRamWidget(QWidget):    def __init__(self):        super().__init__()        self.setWindowTitle("DiagnÃ³stico de MemÃ³ria RAM")        layout = QVBoxLayout()        mem = psutil.virtual_memory()        total = round(mem.total / (1024**3), 2)        available = round(mem.available / (1024**3), 2)        used = round(mem.used / (1024**3), 2)        percent = mem.percent        layout.addWidget(QLabel(f"MemÃ³ria Total: {total} GB"))        layout.addWidget(QLabel(f"MemÃ³ria DisponÃ­vel: {available} GB"))        layout.addWidget(QLabel(f"MemÃ³ria Usada: {used} GB ({percent}%)"))        self.setLayout(layout)