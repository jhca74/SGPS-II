from PyQt5.QtWidgets import QWidget, QLabel, QVBoxLayoutimport platformimport psutilclass DiagnosticoSistemaWidget(QWidget):    def __init__(self):        super().__init__()        self.setWindowTitle("DiagnÃ³stico do Sistema Operacional")        layout = QVBoxLayout()        info = []        info.append(f"Sistema Operacional: {platform.system()} {platform.release()}")        info.append(f"Arquitetura: {platform.machine()}")        info.append(f"Processador: {platform.processor()}")        info.append(f"NÃºcleos (fÃ­sicos): {psutil.cpu_count(logical=False)}")        info.append(f"NÃºcleos (lÃ³gicos): {psutil.cpu_count(logical=True)}")        info.append(f"MemÃ³ria RAM Total: {round(psutil.virtual_memory().total / (1024**3), 2)} GB")        for linha in info:            layout.addWidget(QLabel(linha))        self.setLayout(layout)