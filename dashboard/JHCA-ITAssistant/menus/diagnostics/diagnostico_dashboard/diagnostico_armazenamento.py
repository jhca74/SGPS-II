from PyQt5.QtWidgets import QWidget, QLabel, QVBoxLayoutimport psutilclass DiagnosticoArmazenamentoWidget(QWidget):    def __init__(self):        super().__init__()        self.setWindowTitle("DiagnÃ³stico de Armazenamento")        layout = QVBoxLayout()        disco = psutil.disk_usage('/')        total = round(disco.total / (1024**3), 2)        usado = round(disco.used / (1024**3), 2)        livre = round(disco.free / (1024**3), 2)        percent = disco.percent        layout.addWidget(QLabel(f"EspaÃ§o Total: {total} GB"))        layout.addWidget(QLabel(f"EspaÃ§o Usado: {usado} GB ({percent}%)"))        layout.addWidget(QLabel(f"EspaÃ§o Livre: {livre} GB"))        self.setLayout(layout)