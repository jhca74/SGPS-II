from PyQt5.QtWidgets import QDialog, QVBoxLayout, QLabel, QTextEdit, QPushButtonfrom PyQt5.QtCore import Qtfrom interface.chatbot_info import ChatbotPX, IACentralclass ChatbotInformativo(QDialog):    def __init__(self, nome_ferramenta):        super().__init__()        self.setWindowTitle(f"Assistente Virtual - {nome_ferramenta}")        self.setMinimumSize(500, 300)        layout = QVBoxLayout()        self.pergunta_label = QLabel(f"Pergunta sobre: {nome_ferramenta}")        layout.addWidget(self.pergunta_label)        self.resposta_area = QTextEdit()        self.resposta_area.setReadOnly(True)        layout.addWidget(self.resposta_area)        self.fechar_btn = QPushButton("Fechar")        self.fechar_btn.clicked.connect(self.close)        layout.addWidget(self.fechar_btn, alignment=Qt.AlignRight)        self.setLayout(layout)        # IA integrada        self.chatbot = ChatbotPX(IACentral())        self.mostrar_resposta(nome_ferramenta)    def mostrar_resposta(self, nome_ferramenta):        resposta = self.chatbot.responder(f"O que faz a ferramenta {nome_ferramenta}?")        self.resposta_area.setText(resposta)