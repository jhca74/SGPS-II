from PyQt5.QtWidgets import QWidget, QLabel, QLineEdit, QPushButton, QVBoxLayoutfrom PyQt5.QtGui import QPixmap, QPalette, QBrushfrom PyQt5.QtCore import Qtclass LoginWindow(QWidget):    def __init__(self):        super().__init__()        self.setWindowTitle("Login")        self.setGeometry(100, 100, 600, 400)        # Define imagem de fundo        background = QPixmap("ui/assets/jhca_logo.png")        palette = QPalette()        palette.setBrush(QPalette.Window, QBrush(background.scaled(self.size(), Qt.KeepAspectRatioByExpanding)))        self.setPalette(palette)        # Campos de entrada        self.email_input = QLineEdit(self)        self.email_input.setPlaceholderText("Email")        self.password_input = QLineEdit(self)        self.password_input.setPlaceholderText("Senha")        self.password_input.setEchoMode(QLineEdit.Password)        # Boṭo de login        self.login_button = QPushButton("Entrar", self)        self.login_button.clicked.connect(self.verificar_login)        # Layout vertical centralizado        layout = QVBoxLayout()        layout.addStretch()        layout.addWidget(self.email_input, alignment=Qt.AlignCenter)        layout.addWidget(self.password_input, alignment=Qt.AlignCenter)        layout.addWidget(self.login_button, alignment=Qt.AlignCenter)        layout.addStretch()        self.setLayout(layout)    def verificar_login(self):        if self.email_input.text() == "comercial.jorge.anjos@gmail.com" and self.password_input.text() == "212140043":            print("Login bem-sucedido!")            self.close()        else:            print("Credenciais incorretas.")