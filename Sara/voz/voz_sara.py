import pyttsx3class VozSara:    def __init__(self):        self.engine = pyttsx3.init()        self.engine.setProperty("rate", 165)        self.engine.setProperty("volume", 1)    def falar(self, texto):        self.engine.say(texto)        self.engine.runAndWait()