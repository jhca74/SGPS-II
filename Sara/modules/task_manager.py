from datetime import datetimeimport sqlite3import shutildef backup():    """Backup diÃ¡rio dos arquivos crÃ­ticos"""    today = datetime.now().strftime("%Y%m%d")    shutil.make_archive(f"backups/sara_{today}", 'zip', "data")    return f"Backup criado em backups/sara_{today}.zip"def analyze_tasks():    """Analisa tarefas pendentes no banco de dados"""    conn = sqlite3.connect('sara.db')    cursor = conn.cursor()    pending = cursor.execute("SELECT COUNT(*) FROM tasks WHERE status='pending'").fetchone()[0]    conn.close()    return f"Tarefas pendentes: {pending}"