from datetime import datetimeimport osLOG_PATH = "logs/interacoes.log"os.makedirs(os.path.dirname(LOG_PATH), exist_ok=True)def registar_interacao(pergunta, resposta, origem):    timestamp = datetime.now().isoformat()    linha = f"[{timestamp}] ORIGEM: {origem} | PERGUNTA: {pergunta} | RESPOSTA: {resposta}\n"    with open(LOG_PATH, "a", encoding="utf-8") as f:        f.write(linha)