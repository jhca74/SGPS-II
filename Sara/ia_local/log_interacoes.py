import osfrom datetime import datetimeLOG_DIR = "logs"LOG_FILE = os.path.join(LOG_DIR, "ia_local_log.txt")def registar_interacao(prompt: str, resposta: str):    os.makedirs(LOG_DIR, exist_ok=True)    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")    with open(LOG_FILE, "a", encoding="utf-8") as f:        f.write(f"[{timestamp}] Pergunta: {prompt}\n")        f.write(f"[{timestamp}] Resposta: {resposta}\n\n")