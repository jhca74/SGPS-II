import requestsimport jsonAPI_URL = "http://localhost:8000/resolver"def main():    print("=== Teste do N̼cleo Cloud (/resolver) ===")    while True:        pergunta = input("Tu: ").strip()        if pergunta.lower() in ["sair", "exit", "quit"]:            break        try:            resposta = requests.post(API_URL, json={"texto": pergunta})            resposta.raise_for_status()            dados = resposta.json()            print(f"Sara: {dados['resposta']}")        except Exception as e:            print(f"[ERRO] {e}")if __name__ == "__main__":    main()