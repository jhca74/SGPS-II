from langchain.tools import toolfrom core import compendiofrom rich.console import Consoleconsole = Console()@tooldef guardar_no_compendio(texto: str) -> str:    """Guarda uma frase ou conte̼do no comp̻ndio com origem 'cloud'."""    try:        id_armazenado = compendio.adicionar_entrada(texto, {"origem": "cloud"})        console.log(f"[cloud] Guardado no comp̻ndio com ID {id_armazenado}")        return f"Guardado com sucesso no comp̻ndio com ID {id_armazenado}."    except Exception as e:        console.log(f"[cloud] Erro ao guardar no comp̻ndio: {e}")        return f"Erro ao guardar: {str(e)}"