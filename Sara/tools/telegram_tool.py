from langchain.tools import toolimport requestsimport osfrom dotenv import load_dotenvfrom rich.console import Consoleconsole = Console()load_dotenv()TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID")@tooldef enviar_telegram(mensagem: str) -> str:    """    Envia uma mensagem para o Telegram do utilizador atravÃ©s do bot da Sara.    Requer TELEGRAM_BOT_TOKEN e TELEGRAM_CHAT_ID definidos no .env.    """    if not TELEGRAM_BOT_TOKEN or not TELEGRAM_CHAT_ID:        erro = "âŒ Token ou Chat ID do Telegram nÃ£o definidos no .env"        console.log(f"[telegram] {erro}")        return erro    try:        url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage"        data = {"chat_id": TELEGRAM_CHAT_ID, "text": mensagem}        response = requests.post(url, data=data)        if response.status_code == 200:            console.log("[telegram] âœ… Mensagem enviada com sucesso")            return "Mensagem enviada com sucesso via Telegram."        else:            erro = f"âŒ Erro ao enviar: {response.text}"            console.log(f"[telegram] {erro}")            return erro    except Exception as e:        erro = f"âŒ ExceÃ§Ã£o: {str(e)}"        console.log(f"[telegram] {erro}")        return erro