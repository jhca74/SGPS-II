import jsonfrom datetime import datetimeclass MemoriaEmocional:    def __init__(self, caminho_ficheiro="memoria/emocional.json"):        self.caminho_ficheiro = caminho_ficheiro        self.estado = {"emocao": "neutro", "intensidade": 0, "ultima_entrada": None}        self._carregar()    def _carregar(self):        try:            with open(self.caminho_ficheiro, "r", encoding="utf-8") as f:                self.estado = json.load(f)        except FileNotFoundError:            self._guardar()    def _guardar(self):        with open(self.caminho_ficheiro, "w", encoding="utf-8") as f:            json.dump(self.estado, f, ensure_ascii=False, indent=2)    def atualizar(self, emocao, intensidade):        self.estado["emocao"] = emocao        self.estado["intensidade"] = intensidade        self.estado["ultima_entrada"] = datetime.now().isoformat()        self._guardar()    def obter_estado(self):        return self.estado