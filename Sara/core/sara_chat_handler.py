import threadingimport timeclass SaraChatHandler:    def __init__(self):        self.conversa = []        self.ativa = True    def enviar_mensagem(self, texto):        print(f"[Utilizador] {texto}")        resposta = self.gerar_resposta(texto)        self.conversa.append((texto, resposta))        print(f"[Sara] {resposta}")    def gerar_resposta(self, texto):        if "ola" in texto.lower():            return "OlÃ¡! Como posso ajudar?"        elif "parar" in texto.lower():            self.ativa = False            return "Vou ficar em silÃªncio por agora."        return "Ainda estou a aprender sobre isso."    def iniciar_conversa(self):        print("[SaraChatHandler] SessÃ£o iniciada.")        while self.ativa:            texto = input("Tu: ")            self.enviar_mensagem(texto)            time.sleep(0.5)if __name__ == "__main__":    chat = SaraChatHandler()    threading.Thread(target=chat.iniciar_conversa).start()