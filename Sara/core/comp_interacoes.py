import datetimeclass LogInteracoes:    def __init__(self, ficheiro_log="logs/interacoes.txt"):        self.ficheiro_log = ficheiro_log    def registar_interacao(self, entrada, resposta, origem):        timestamp = datetime.datetime.now().isoformat()        log_line = f"[{timestamp}] ({origem}) Pergunta: {entrada} => Resposta: {resposta}\n"        try:            with open(self.ficheiro_log, "a", encoding="utf-8") as f:                f.write(log_line)        except Exception as e:            print(f"[LogInteracoes] Erro ao escrever no log: {e}")