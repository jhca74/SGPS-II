import osimport platformimport subprocessdef abrir_pasta(path):    try:        if platform.system() == "Windows":            os.startfile(path)        elif platform.system() == "Darwin":            subprocess.call(["open", path])        else:            subprocess.call(["xdg-open", path])        print(f"[Sistema] Pasta aberta: {path}")    except Exception as e:        print(f"[Erro] NÃ£o foi possÃ­vel abrir a pasta: {e}")def desligar_pc():    sistema = platform.system()    try:        if sistema == "Windows":            os.system("shutdown /s /t 1")        elif sistema == "Linux" or sistema == "Darwin":            os.system("shutdown now")        print("[Sistema] Desligamento iniciado.")    except Exception as e:        print(f"[Erro] Falha ao tentar desligar: {e}")