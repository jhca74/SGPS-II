import osimport requestsfrom dotenv import load_dotenvload_dotenv(dotenv_path=".env_codigos")TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID")def enviar_para_telegram(mensagem: str) -> None:    if not TELEGRAM_BOT_TOKEN or not TELEGRAM_CHAT_ID:        print("[Telegram] Token ou Chat ID em falta.")        return    url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage"    payload = {        "chat_id": TELEGRAM_CHAT_ID,        "text": mensagem    }    try:        response = requests.post(url, data=payload, timeout=10)        response.raise_for_status()    except Exception as e:        print(f"[Telegram] Falha ao enviar: {e}")