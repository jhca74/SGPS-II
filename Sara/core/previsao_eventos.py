# previsao_eventos.pyfrom datetime import datetime, timedeltadef prever_evento(base_dados):    """    base_dados: lista de dicionÃ¡rios com {'tipo': 'email'/'gasto'/'scraping', 'data': datetime}    """    agora = datetime.now()    avisos = []    futuros = {        'scraping_manha': any(item['tipo'] == 'scraping' and item['data'].hour < 10 for item in base_dados),        'gasto_recorrente': sum(1 for item in base_dados if item['tipo'] == 'gasto' and item['data'].day >= 25) >= 2,        'atividade_nocturna': sum(1 for item in base_dados if item['data'].hour >= 22) >= 3,    }    if not futuros['scraping_manha']:        avisos.append("âš ï¸ NÃ£o foi feito scraping de manhÃ£ nos Ãºltimos dias.")    if futuros['gasto_recorrente']:        avisos.append("ðŸ’¸ PadrÃ£o de gastos elevados ao final do mÃªs.")    if futuros['atividade_nocturna']:        avisos.append("ðŸŒ™ Muitas interaÃ§Ãµes noturnas. Queres mudar o horÃ¡rio dos alertas?")    return avisos or ["Nenhum padrÃ£o crÃ­tico detetado."]