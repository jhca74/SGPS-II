from fastapi import APIRouterfrom core.autoajuste_cognitivo import AutoAjusteCognitivorouter = APIRouter()# InstÃ¢ncia global opcional (pode ser substituÃ­da por estado global mais complexo)ajuste_global = AutoAjusteCognitivo()@router.post("/autoajuste")def autoajustar():    ajuste_global.ajustar_comportamento()    return {"mensagem": "Autoajuste cognitivo executado com sucesso."}@router.post("/autoajuste/emocao")def atualizar_emocao(novo_estado: str):    ajuste_global.atualizar_estado_emocional(novo_estado)    return {"mensagem": f"Estado emocional atualizado para '{novo_estado}'"}