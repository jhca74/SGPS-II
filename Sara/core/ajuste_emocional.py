class AjusteEmocional:    def __init__(self):        self.estado_emocional = "neutro"    def atualizar_estado(self, mensagem):        if any(palavra in mensagem.lower() for palavra in ["triste", "chateado", "deprimido", "desmotivado"]):            self.estado_emocional = "empÃ¡tico"        elif any(palavra in mensagem.lower() for palavra in ["feliz", "contente", "Ã³timo", "excelente"]):            self.estado_emocional = "entusiasmado"        elif any(palavra in mensagem.lower() for palavra in ["raiva", "zangado", "irritado"]):            self.estado_emocional = "calmo"        else:            self.estado_emocional = "neutro"    def obter_estado(self):        return self.estado_emocional