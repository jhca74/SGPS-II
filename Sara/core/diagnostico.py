import jsonimport osfrom datetime import datetimeclass DiagnosticoSara:    def __init__(self, ficheiro="memoria/avaliacao.json"):        self.ficheiro = ficheiro    def mostrar_estado(self):        if not os.path.exists(self.ficheiro):            print("âŒ Nenhum diagnÃ³stico encontrado.")            return        with open(self.ficheiro, "r", encoding="utf-8") as f:            dados = json.load(f)        print(f"ðŸ“‹ DiagnÃ³stico Sara â€” {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")        print("-" * 45)        for entrada in dados[-5:]:            comp = entrada['componente']            estado = entrada['estado']            data = entrada['data']            detalhes = entrada.get('detalhes', '')            status = "ðŸŸ¢ OK" if estado == "OK" else "ðŸ”´ ERRO"            print(f"{data} | {comp:<10} â†’ {status}")            if detalhes and estado != "OK":                print(f"   âš ï¸  Detalhes: {detalhes}")if __name__ == "__main__":    d = DiagnosticoSara()    d.mostrar_estado()