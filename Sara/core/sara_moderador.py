import randomclass SaraModeradora:    def __init__(self):        self.modelos = ["GPT", "Claude", "Gemini", "DeepSeek", "LangChain"]        self.rondas = []    def simular_resposta(self, modelo, pergunta, contexto=""):        return f"[{modelo}] Em resposta a '{pergunta}' com base em '{contexto}': SimulaÃ§Ã£o de ideia."    def conduzir_brainstorm(self, pergunta):        log = []        contexto = pergunta        for ronda in range(1, 4):  # mÃ­nimo 3 rondas            respostas_ronda = {}            log.append(f"ðŸ§  Ronda {ronda}:")            for modelo in self.modelos:                resposta = self.simular_resposta(modelo, pergunta, contexto)                respostas_ronda[modelo] = resposta                log.append(f"  - {modelo}: {resposta}")            contexto = " ".join(respostas_ronda.values())  # nova base para prÃ³xima ronda            self.rondas.append(respostas_ronda)        return {            "pergunta_inicial": pergunta,            "modelo_cientifico": {                "analise": pergunta,                "hipotese": "Conjunto de ideias combinadas entre IAs",                "solucao": contexto,                "teste": "Simulado",                "conclusao": "SÃ­ntese das melhores ideias apÃ³s 3 rondas"            },            "rondas_log": log        }