import subprocessfrom PyQt5.QtWidgets import QWidget, QVBoxLayout, QTextEdit, QPushButtonclass LinuxMenu(QWidget):    def __init__(self):        super().__init__()        self.setWindowTitle("Ferramentas Linux")        layout = QVBoxLayout()        self.output = QTextEdit()        self.output.setReadOnly(True)        layout.addWidget(self.output)        btn_disk = QPushButton("Ver Espa̤o em Disco")        btn_disk.clicked.connect(self.mostrar_espaco_disco)        layout.addWidget(btn_disk)        btn_particoes = QPushButton("Ver Parti̵̤es")        btn_particoes.clicked.connect(self.mostrar_particoes)        layout.addWidget(btn_particoes)        btn_pacotes = QPushButton("Listar Pacotes Instalados (APT)")        btn_pacotes.clicked.connect(self.listar_pacotes)        layout.addWidget(btn_pacotes)        self.setLayout(layout)    def mostrar_espaco_disco(self):        resultado = subprocess.getoutput("df -h")        self.output.setText(resultado)    def mostrar_particoes(self):        resultado = subprocess.getoutput("lsblk")        self.output.setText(resultado)    def listar_pacotes(self):        resultado = subprocess.getoutput("dpkg -l | head -n 50")        self.output.setText(resultado)