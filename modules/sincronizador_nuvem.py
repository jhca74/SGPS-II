class SincronizadorNuvem:    def __init__(self):        self.capacidade_maxima = 1000        self.ocupado = 800        def verificar_uso(self):        return self.ocupado / self.capacidade_maxima        def sincronizar(self, dados):        if self.verificar_uso() > 0.8:            print("Enviando dados antigos para a nuvem...")            return True        else:            print("Espa̤o suficiente localmente.")            return False